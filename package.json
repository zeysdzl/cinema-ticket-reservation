{
  "name": "cinema-ticket-reservation",
  "version": "1.2.0",
  "description": "Cinema ticket reservation app (file storage, Node.js, JWT, Swagger, Docker) with new UI, register/login, cart, checkout.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "lint": "echo \"(optional) add eslint later\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "swagger-ui-express": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
